# Raspberry Pi voice client dependencies
#
# INSTALL ORDER MATTERS — run these two commands in sequence:
#
#   pip install openwakeword --no-deps
#   pip install -r requirements.txt
#
# Why: openwakeword declares tflite-runtime as a dependency but tflite has no
# Python 3.13 wheel for ARM64. We use inference_framework="onnx" so tflite is
# never loaded at runtime — installing without deps skips it safely.

# openwakeword ONNX-backend runtime deps (replaces tflite)
onnxruntime>=1.10.0,<2
scipy>=1.3,<2
scikit-learn>=1,<2
tqdm>=4.0,<5

# Audio I/O
sounddevice>=0.4.6
numpy>=1.24

# HTTP client
requests>=2.31

# TTS — Piper (high quality neural, primary) + pyttsx3 (espeak fallback)
piper-tts>=1.2.0
pyttsx3>=2.90

# Required by pyttsx3 on Linux/RPi (install system package first):
#   sudo apt install espeak espeak-data libespeak-dev
